-- Bufferline
require('bufferline').setup({
    options = {
        hover = {
            enabled = true,
            delay = 200,
            reveal = {'close'}
        },
        mode = 'tabs',
        show_buffer_close_icons = false,
        show_close_icon = false
    }
})
